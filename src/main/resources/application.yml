spring:
# database
  datasource:
    url: jdbc:mysql://localhost:3306/booking_system?useSSL=false&serverTimezone=UTC
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

# Redis
  data:
    redis:
       host: localhost
       port: 6379
       database: 0


  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true

aliyun:
  mail:
    region: cn-hangzhou
    from-address: service@mail.fulingyi.xyz